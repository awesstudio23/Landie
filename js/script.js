$(document).ready(function () {
  $.scrollify({
    section: ".scrollify",
    scrollSpeed: 100,
    setHeights: false,
    updateHash: false,
    touchScroll: true,
  });
  if (e.cancelable) {
    e.preventDefault();
  }
});
